# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# This file only contains values that have been changed from the defaults.
#
# For a description of this file, see:
#   http://www.postgresql.org/docs/8.4/static/auth-pg-hba-conf.html
#
# For a full version of this file with comments, see:
#   http://git.postgresql.org/gitweb/?p=postgresql.git;a=blob;f=src/backend/libpq/pg_hba.conf.sample;hb=REL8_4_STABLE

# Allow all users to connect over the local socket
local   all         all                                 trust

# Allow all users to connect over the network with valid credentials
host    all         all           <%= ENV['OPENSHIFT_PG_HOST'] %>/32             trust
host    all         all           ::1/128               md5
